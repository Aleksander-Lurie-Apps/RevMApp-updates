# RevMApp update information

This repository contains only public version metadata for RevMApp.

`latest.json` identifies the latest complete test build for macOS Apple Silicon and Windows x64. The download link points to a private GitHub release and requires authorized access.

Application source code, installers, integration configuration and user data are not stored here. GitHub Actions is disabled for this repository; the private application's release workflow updates only `latest.json`.
